import { UploadForm } from "@/components/upload-form"; // Import the new component

export default function UploadPage() {
  return (
    <div className="py-6 md:py-10">
      {/* You can add a heading or more context if needed */}
      <UploadForm />
    </div>
  );
}
